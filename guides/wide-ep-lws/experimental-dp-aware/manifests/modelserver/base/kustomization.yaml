apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - decode.yaml
  - prefill.yaml
  - serviceAccount.yaml
  - pod-monitors.yaml  # comment out if Prometheus Operator CRDs (PodMonitor) are not installed on the cluster
