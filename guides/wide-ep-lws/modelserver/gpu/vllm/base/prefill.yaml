apiVersion: leaderworkerset.x-k8s.io/v1
kind: LeaderWorkerSet
metadata:
  name: wide-ep-llm-d-prefill
  labels:
    llm-d.ai/inference-serving: "true"
    llm-d.ai/guide: "wide-ep-lws"
    llm-d.ai/accelerator-variant: "gpu"
    llm-d.ai/accelerator-vendor: "nvidia"
    llm-d.ai/role: prefill
spec:
  replicas: 1
  leaderWorkerTemplate:
    size: 2
    workerTemplate:
      metadata:
        labels:
          llm-d.ai/inference-serving: "true"
          llm-d.ai/guide: "wide-ep-lws"
          llm-d.ai/accelerator-variant: "gpu"
          llm-d.ai/accelerator-vendor: "nvidia"
          llm-d.ai/role: prefill
      spec:
        serviceAccountName: glm
        volumes:
          - name: dshm
            emptyDir:
              medium: Memory
              sizeLimit: 2Gi
          - name: hf-cache
            emptyDir: {}
          - name: jit-cache
            emptyDir: {}
          - name: profiles
            emptyDir: {}
        containers:
          - name: vllm
            # image: docker.io/vllm/vllm-openai:kimi-k3
            image: public.ecr.aws/q9t5s3a7/vllm-ci-test-repo:5619fd84968331f6900a86403f23a2866641265a@sha256:7c723236a35c2afab1c135fdbcae7a859fafa3a788eaf51357a6e80010f1c07c
            securityContext:
              capabilities:
                add:
                  - IPC_LOCK
                  - SYS_RAWIO
              runAsGroup: 0
              runAsUser: 0
              privileged: true
            imagePullPolicy: Always
            command:
              - /bin/bash
              - -c
            args:
              - |-
                START_RANK=$(( ${LWS_WORKER_INDEX:-0} * DP_SIZE_LOCAL ))
                export UCX_IB_ROCE_REACHABILITY_MODE=all
                export VLLM_USE_V2_MODEL_RUNNER=1
                export VLLM_SSM_CONV_STATE_LAYOUT=DS
                export VLLM_ENABLE_K3_LATENT_MOE_TAIL_FUSION=1
                export NCCL_GIN_TYPE=3
                export VLLM_DEEPEP_V2_ALLOW_HYBRID_MODE=1
                export VLLM_PREFIX_CACHE_RETENTION_INTERVAL=0

                # --- BEGIN TEMP: install NIXL ----------------------------------------
                uv pip install --system nixl nixl-cu13
                uv pip show nvidia-nccl-cu13
                # --- END TEMP: install NIXL ------------------------------------------

                apt-get update && apt-get install -y infiniband-diags

                apt-get update && apt-get install -y \
                  git \
                  build-essential \
                  cmake \
                  ninja-build \
                  pkg-config \
                  libudev-dev \
                  libnl-3-dev \
                  libnl-route-3-dev
                
                # NVSHMEM 3.4.5's IBGDA control-buffer DMA-BUF path is compiled
                # conditionally from rdma-core headers. Installing newer runtime
                # libraries alone is insufficient: the v44 headers must be visible when
                # NVSHMEM is configured and compiled.
                RDMA_CORE_VERSION=v44.0
                export RDMA_CORE_PREFIX=/opt/rdma-core-44

                rm -rf /tmp/rdma-core-44 "${RDMA_CORE_PREFIX}"

                git clone \
                  --depth 1 \
                  --branch "${RDMA_CORE_VERSION}" \
                  https://github.com/linux-rdma/rdma-core.git \
                  /tmp/rdma-core-44

                git -C /tmp/rdma-core-44 describe --tags --always

                cmake -S /tmp/rdma-core-44 \
                  -B /tmp/rdma-core-44/build-install \
                  -GNinja \
                  -DCMAKE_BUILD_TYPE=Release \
                  -DCMAKE_INSTALL_PREFIX="${RDMA_CORE_PREFIX}" \
                  -DCMAKE_INSTALL_LIBDIR=lib \
                  -DNO_MAN_PAGES=1

                cmake --build /tmp/rdma-core-44/build-install \
                  --parallel "$(nproc)"

                cmake --install /tmp/rdma-core-44/build-install

                find /opt/rdma-core-44 \
                  \( -name 'libibverbs.so*' -o -name 'libmlx5.so*' \) \
                  -print

                LD_LIBRARY_PATH=/opt/rdma-core-44/lib \
                  /opt/rdma-core-44/bin/ibv_devices

                export LD_LIBRARY_PATH=/opt/rdma-core-44/lib:$LD_LIBRARY_PATH

                exec vllm serve \
                  moonshotai/Kimi-K3 \
                  --port 8000 \
                  --trust-remote-code \
                  --gpu-memory-utilization 0.9 \
                  --disable-access-log-for-endpoints=/health,/metrics \
                  --max-num-batched-tokens 8192 \
                  --max-num-seqs 16 \
                  --speculative-config '{"model":"Inferact/Kimi-K3-DSpark","method":"dspark","num_speculative_tokens":1,"attention_backend":"FLASHINFER_MLA","draft_sample_method":"probabilistic","rejection_sample_method":"block"}' \
                  --enable-prefix-caching \
                  --enable-expert-parallel \
                  --tensor-parallel-size $TP_SIZE \
                  --data-parallel-hybrid-lb \
                  --data-parallel-size $((LWS_GROUP_SIZE * DP_SIZE_LOCAL)) \
                  --data-parallel-size-local $DP_SIZE_LOCAL \
                  --data-parallel-address ${LWS_LEADER_ADDRESS} \
                  --data-parallel-rpc-port 5555 \
                  --data-parallel-start-rank $START_RANK \
                  --no-disable-hybrid-kv-cache-manager \
                  --all2all-backend deepep_v2 \
                  --enforce-eager \
                  --moe-backend auto \
                  --max-model-len 256000 \
                  --profiler-config.profiler=torch \
                  --profiler-config.torch_profiler_dir=/profiles \
                  --profiler-config.torch_profiler_with_stack=False \
                  --profiler-config.max_iterations=10 \
                  --profiler-config.ignore_frontend=true \
                  --kv_transfer_config '{"kv_connector":"NixlConnector",
                        "kv_role":"kv_producer",
                        "kv_load_failure_policy":"fail"}' \
                  --no-enable-flashinfer-autotune \
                  --kv-cache-dtype fp8 \
                  --attention-config '{"use_prefill_query_quantization":true,"mla_prefill_backend":"flashinfer"}'

            env:
              - name: HF_TOKEN
                valueFrom:
                  secretKeyRef:
                    name: llm-d-hf-token
                    key: HF_TOKEN
              # To enable tracing, uncomment:
              # - name: OTEL_SERVICE_NAME
              #   value: "vllm-prefill"
              # - name: OTEL_EXPORTER_OTLP_ENDPOINT
              #   value: "http://otel-collector:4317"
              # - name: OTEL_TRACES_SAMPLER
              #   value: "parentbased_traceidratio"
              # - name: OTEL_TRACES_SAMPLER_ARG
              #   value: "0.1"
              - name: VLLM_ENGINE_READY_TIMEOUT_S
                value: "3000"
              - name: VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS
                value: "1"
              - name: VLLM_RANDOMIZE_DP_DUMMY_INPUTS
                value: "1"
              - name: DP_SIZE_LOCAL
                value: "1"
              - name: TP_SIZE
                value: "8"
              - name: TRITON_LIBCUDA_PATH
                value: /usr/local/nvidia/lib64
              - name: VLLM_SKIP_P2P_CHECK
                value: "1"
              - name: NVIDIA_GDRCOPY
                value: enabled
              # `init failed for transport: IBGDA` is only the summary line; the
              # reason it failed (device open, DEVX, GPU memory registration, ...)
              # is logged one level down. Subsys deliberately unset so this covers
              # every subsystem at INFO.
              - name: NVSHMEM_DEBUG
                value: INFO
              - name: NVSHMEM_REMOTE_TRANSPORT
                value: ibgda
              - name: NVSHMEM_IB_ENABLE_IBGDA
                value: "true"
              # Keep the modern CUDA DMA-BUF registration path enabled. The NVSHMEM
              # build above hard-fails if its IBGDA plugin was compiled without it.
              - name: NVSHMEM_DISABLE_DMABUF
                value: "0"
              # Diagnostic fallback only. Enabling this masks a build without the
              # GPU-resident NIC-buffer DMA-BUF path:
              # - name: NVSHMEM_IBGDA_FORCE_NIC_BUF_MEMTYPE
              #   value: hostmem
              - name: NVSHMEM_BOOTSTRAP_UID_SOCK_IFNAME
                value: eth0
              - name: GLOO_SOCKET_IFNAME
                value: eth0
              - name: NCCL_SOCKET_IFNAME
                value: eth0
              - name: VLLM_LOGGING_LEVEL
                value: INFO
              # Default keep-alive timeout of 5s is shorter than the sidecar's 90s idle conn timeout,
              # causing TCP RSTs.
              - name: VLLM_HTTP_TIMEOUT_KEEP_ALIVE
                value: "120"
              - name: VLLM_NIXL_SIDE_CHANNEL_HOST
                valueFrom:
                  fieldRef:
                    fieldPath: status.podIP

              # Use cache directories from the mounted volume under one easy to mount
              # root directory.
              - name: CUDA_CACHE_PATH
                value: /var/cache/vllm/cuda
              - name: CCACHE_DIR
                value: /var/cache/vllm/ccache
              - name: VLLM_CACHE_ROOT
                value: /var/cache/vllm/vllm
              - name: FLASHINFER_WORKSPACE_BASE
                value: /var/cache/vllm/flashinfer
              # HuggingFace is likely to be quite a bit larger, give it its own cache
              - name: HF_HUB_CACHE
                value: /var/cache/huggingface
            ports:
              - containerPort: 8000
                name: modelserver
                protocol: TCP
            startupProbe:
              httpGet:
                path: /health
                port: modelserver
              initialDelaySeconds: 0
              periodSeconds: 1
              timeoutSeconds: 5
              failureThreshold: 2700
            livenessProbe:
              httpGet:
                path: /health
                port: modelserver
              periodSeconds: 30
              timeoutSeconds: 5
              failureThreshold: 3
            readinessProbe:
              httpGet:
                path: /health
                port: modelserver
              periodSeconds: 10
              timeoutSeconds: 5
              failureThreshold: 3
            resources:
              limits:
                ephemeral-storage: 1Ti
                memory: 256Gi
                nvidia.com/gpu: "8"
              requests:
                cpu: 64
                ephemeral-storage: 1Ti
                memory: 256Gi
                nvidia.com/gpu: "8"
            volumeMounts:
              - name: dshm
                mountPath: /dev/shm
              - name: hf-cache
                mountPath: /var/cache/huggingface
              - name: jit-cache
                mountPath: /var/cache/vllm
              - name: profiles
                mountPath: /profiles
