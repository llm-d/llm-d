# Autoscaling Workloads with KEDA and EPP Metrics

> **Moved.** This guide has been consolidated into
> [`keda-epp/`](../keda-epp/README.md). The queue-depth signal is the default
> there - apply the `k8s-queue` (or `ocp-queue`) overlay.
