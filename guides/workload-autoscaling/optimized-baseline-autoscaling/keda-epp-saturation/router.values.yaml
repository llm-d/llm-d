## Enables EPP flow control for KEDA saturation-based autoscaling.
## Flow control is the source of llm_d_epp_flow_control_pool_saturation. This layers
## ONLY the `flowControl` feature gate onto the optimized-baseline EPP config, reusing
## its existing plugins unchanged — pool saturation is a flow-control-layer metric and
## does not depend on the scoring plugins. (The `featureGates` gate lives inside the EPP
## config file, so it is set alongside the same plugins optimized-baseline already runs.)
## Layer AFTER optimized-baseline/router/optimized-baseline.values.yaml.

router:
  epp:
    pluginsConfigFile: "optimized-baseline-plugins.yaml"
    pluginsCustomConfig:
      optimized-baseline-plugins.yaml: |
        apiVersion: llm-d.ai/v1alpha1
        kind: EndpointPickerConfig
        featureGates:
        - flowControl
        # Same plugins as optimized-baseline/router/optimized-baseline.values.yaml —
        # this override only adds the flowControl feature gate above.
        plugins:
        - type: approx-prefix-cache-producer
        - type: inflight-load-producer
        - type: prefix-cache-affinity-filter
        - type: token-load-scorer
        schedulingProfiles:
        - name: default
          plugins:
          - pluginRef: prefix-cache-affinity-filter
          - pluginRef: token-load-scorer
