apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # TriggerAuthentication first: the ScaledObject's trigger references it.
  - wva-triggerauthentication.yaml
  - wva-scaledobject.yaml
