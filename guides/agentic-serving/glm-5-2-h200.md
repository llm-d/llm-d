# Agentic Code Generation — GLM-5.2-FP8 on H200

This is one of the accelerator-specific deployments of the agentic code-generation workload; see the
[agentic-serving README](README.md#deployments) for the workload framing and the alternatives:
[NVIDIA-Nemotron-3-Ultra-550B on H200](nemotron-3-ultra-550b-h200.md),
[Qwen3-Coder-480B on TPU v7](qwen3-coder-480b-tpu.md).

## Overview

This guide deploys [GLM-5.2-FP8](https://huggingface.co/zai-org/GLM-5.2-FP8) (753B MoE) on H200
GPUs using P/D-disaggregated LeaderWorkerSets with NIXL for KV transfer. Prefill runs
DEP8 (TP=1, DP=8) on 1 node; decode runs DEP16 (TP=1, DP=16) across 2 nodes (wide EP).
DeepEP high-throughput all-to-all for prefill, low-latency for decode.

DeepGemm MoE backend, tool calling (`glm47`) and reasoning (`glm45`) parsers.
DSpark speculative decoding is available on multi-node deployments via `ENABLE_DSPARK=1`.

This deployment uses the [wide-ep-lws](../wide-ep-lws/README.md) guide's manifests and
was tested on CoreWeave (CKS) with InfiniBand networking.

## Default Configuration

| Parameter               | Value                                                                              |
| ----------------------- | ---------------------------------------------------------------------------------- |
| Model                   | [zai-org/GLM-5.2-FP8](https://huggingface.co/zai-org/GLM-5.2-FP8)                |
| Accelerator             | NVIDIA H200 (8 GPUs per node)                                                      |
| DP model                | Supervisor (`--data-parallel-multi-port-external-lb`)                              |
| Prefill parallelism     | TP=1, DP=8, EP=8 (DEP8) — 1 node                                                  |
| Decode parallelism      | TP=1, DP=16, EP=16 (DEP16, wide) — 2 nodes                                        |
| All-to-all (prefill)    | `deepep_high_throughput`                                                           |
| All-to-all (decode)     | `deepep_low_latency` (IBGDA + NVSHMEM)                                            |
| MoE backend             | DeepGemm                                                                           |
| KV transfer             | NixlConnector                                                                      |
| Reasoning / tool-call   | glm45 / glm47                                                                     |

### P/D Deployment Options

| Deployment | Prefill                    | Decode                        | Nodes / GPUs |
| ---------- | -------------------------- | ----------------------------- | ------------ |
| `p1w1d1w1` | 1 replica, 1 node, DEP8    | 1 replica, 1 node, DEP8      | 2 / 16       |
| `p1w1d1w2` | 1 replica, 1 node, DEP8    | 1 replica, 2 nodes, DEP16    | 3 / 24       |
| `p1w2d1w2` | 1 replica, 2 nodes, DEP16  | 1 replica, 2 nodes, DEP16    | 4 / 32       |
| `p2w1d1w1` | 2 replicas, 1 node, DEP8   | 1 replica, 1 node, DEP8      | 3 / 24       |
| `p2w1d1w2` | 2 replicas, 1 node, DEP8   | 1 replica, 2 nodes, DEP16    | 4 / 32       |
| `p2w2d1w2` | 2 replicas, 2 nodes, DEP16 | 1 replica, 2 nodes, DEP16    | 6 / 48       |
| `p2w2d2w2` | 2 replicas, 2 nodes, DEP16 | 2 replicas, 2 nodes, DEP16   | 8 / 64       |
| `p3w2d1w2` | 3 replicas, 2 nodes, DEP16 | 1 replica, 2 nodes, DEP16    | 8 / 64       |
| `p3w2d2w2` | 3 replicas, 2 nodes, DEP16 | 2 replicas, 2 nodes, DEP16   | 10 / 80      |

### Supported Hardware Backends

| Backend             | Directory                                                      | Notes                                    |
| ------------------- | -------------------------------------------------------------- | ---------------------------------------- |
| NVIDIA GPU (vLLM)   | `wide-ep-lws/modelserver/gpu/vllm-glm-5.2/`                   | H200, P/D disaggregated                  |

## Prerequisites

This deployment uses the [wide-ep-lws](../wide-ep-lws/README.md) guide infrastructure. Follow the
[wide-ep-lws prerequisites](../wide-ep-lws/README.md#prerequisites) (namespace, HF token secret,
LeaderWorkerSet controller), then set the GLM-5.2-specific variables:

```bash
export KUBECONFIG=~/.kube/config
export NAMESPACE=<your-namespace>
export MODEL=zai-org/GLM-5.2-FP8
```

## Installation Instructions

### 1. Deploy the llm-d Router

Deploy the router from the wide-ep-lws guide:

```bash
export GUIDE_NAME="wide-ep-lws"

helm install ${GUIDE_NAME} \
    ${ROUTER_STANDALONE_CHART} \
    -f ${REPO_ROOT}/guides/recipes/router/base.values.yaml \
    -f ${REPO_ROOT}/guides/${GUIDE_NAME}/router/${GUIDE_NAME}.values.yaml \
    -n ${NAMESPACE} --version ${ROUTER_CHART_VERSION}
```

### 2. Deploy the Model Server

#### P/D Disaggregated

```bash
kubectl apply -n ${NAMESPACE} \
    -k ${REPO_ROOT}/guides/wide-ep-lws/modelserver/gpu/vllm-glm-5.2/deployments/<deployment>
```

Wait for pods to become ready (model load takes time; the startup probe allows up to 45 minutes):

```bash
kubectl get pods -n ${NAMESPACE} -l llm-d.ai/model=GLM-5.2-FP8 -w
```

## Verification

### 1. Get the IP of the Proxy

```bash
export IP=$(kubectl get service ${GUIDE_NAME}-epp -n ${NAMESPACE} -o jsonpath='{.spec.clusterIP}')
```

### 2. Send Test Requests

Open a temporary interactive shell inside the cluster:

```bash
kubectl run curl-debug --rm -it \
    --image=cfmanteiga/alpine-bash-curl-jq \
    --env="IP=$IP" \
    --env="NAMESPACE=$NAMESPACE" \
    -- /bin/bash
```

Send a completion request:

```bash
curl -X POST http://${IP}/v1/completions \
    -H 'Content-Type: application/json' \
    -d '{
        "model": "zai-org/GLM-5.2-FP8",
        "prompt": "Explain how a simple agent loop works in 3 sentences."
    }' | jq
```

## Optional Features

### DSpark Speculative Decoding

Set `ENABLE_DSPARK=1` on both prefill and decode to enable DSpark speculative decoding.
Only activates on multi-node deployments. See the
[model server README](../wide-ep-lws/modelserver/gpu/vllm-glm-5.2/README.md#dspark-speculative-decoding)
for configuration details.

### EPP Routing Overrides

The GLM-5.2 router overrides (`router/glm-5.2-overrides.values.yaml`) add dual prefix-cache
scoring (GPU + CPU) for P/D routing. Include the overrides file when deploying the router:

```bash
helm install wide-ep-lws ${ROUTER_CHART} \
    -f guides/recipes/router/base.values.yaml \
    -f guides/wide-ep-lws/router/wide-ep-lws.values.yaml \
    -f guides/wide-ep-lws/router/glm-5.2-overrides.values.yaml \
    -n ${NAMESPACE}
```

### Monitoring

Node-exporter sidecars are included on each pod for InfiniBand and system metrics.
Apply Prometheus scrape configs with:

```bash
bash guides/wide-ep-lws/monitoring/apply-scrape-configs.sh ${NAMESPACE}
```

## Cleanup

```bash
kubectl delete -n ${NAMESPACE} \
    -k ${REPO_ROOT}/guides/wide-ep-lws/modelserver/gpu/vllm-glm-5.2/deployments/<deployment>
```

**Router:**

```bash
helm uninstall ${GUIDE_NAME} -n ${NAMESPACE}
```
