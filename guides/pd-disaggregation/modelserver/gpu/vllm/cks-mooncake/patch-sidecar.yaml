apiVersion: apps/v1
kind: Deployment
metadata:
  name: decode
spec:
  template:
    spec:
      initContainers:
        - name: routing-proxy
          args:
            - --port=8000
            - --kv-connector=mooncake
            - --zap-log-level=1
            - --secure-serving=false
